# @package _global_

defaults:
  - _self_
  - model: minerals.yaml
  - paths: default.yaml
  - hydra: default.yaml

root: ${hydra:runtime.cwd}

task_name: convert
weights_path: ${root}/logs/train/runs/2022-09-21_20-01-28/checkpoints/epoch_004.ckpt
convert_to: pytorch
save_path: ${root}/weights/resnet18.pt
